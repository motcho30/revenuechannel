---
const features = [
  {
    title: "Marketing",
    description: "Grow your list, build your messaging strategy, deliver excellent customer service, drive purchases, and get 34x ROI on spend — all in one place.",
    color: "blue"
  },
  {
    title: "Sales",
    description: "Double your SMS revenue by pairing our team of expertly-trained sales associates with purpose-built SMS Sales software to proactively engage your subscribers.",
    color: "green"
  },
  {
    title: "CashBack",
    description: "Boost your contribution margin and drive retention by making simple swaps to replace traditional coupons for more profitable CashBack offers.",
    color: "yellow"
  }
];
---

<section class="features">
  <h2>The only SMS revenue platform for Shopify merchants</h2>
  <div class="feature-grid">
    {features.map(feature => (
      <div class={`feature-card ${feature.color}`}>
        <h3>{feature.title}</h3>
        <p>{feature.description}</p>
        <a href="#" class="learn-more">Learn More</a>
      </div>
    ))}
  </div>
</section>

<style>
  .features {
    padding: 4rem 2rem;
  }
  h2 {
    text-align: center;
    font-size: 2.5rem;
    margin-bottom: 2rem;
    background: linear-gradient(135deg, #ffffff, #b3b3b3);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
  }
  .feature-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 2rem;
  }
  .feature-card {
    background-color: rgba(255, 255, 255, 0.1);
    border-radius: 10px;
    padding: 2rem;
    transition: all 0.3s ease;
    border: 1px solid;
  }
  .feature-card:hover {
    transform: translateY(-10px);
  }
  .blue { border-color: #4a00e0; }
  .green { border-color: #00e676; }
  .yellow { border-color: #ffd600; }
  h3 {
    font-size: 1.5rem;
    margin-bottom: 1rem;
  }
  .blue h3 { color: #4a00e0; }
  .green h3 { color: #00e676; }
  .yellow h3 { color: #ffd600; }
  p {
    font-size: 1rem;
    color: #b3b3b3;
    margin-bottom: 1rem;
  }
  .learn-more {
    color: #ffffff;
    text-decoration: none;
    font-weight: bold;
  }
</style>